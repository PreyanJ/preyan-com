// When page loads
document.addEventListener('DOMContentLoaded', function() {
    // Find button
    const btn = document.getElementById('magic-btn');
    // Add click action
    btn.addEventListener('click', function() {
        alert('🧪 CHEMISTRY + CODE = MAGIC! ⚡');
    });
});